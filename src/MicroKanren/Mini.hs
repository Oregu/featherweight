module MicroKanren.Mini where

--import MicroKanren

fresh ∷ a
fresh = undefined

disjAll ∷ a
disjAll = undefined

conjAll ∷ a
conjAll = undefined

conde ∷ a
conde = undefined

run ∷ Int -> [a] -> [a]
run = take

runAll ∷ [a] -> [a]
runAll = id

reify ∷ a
reify = undefined
